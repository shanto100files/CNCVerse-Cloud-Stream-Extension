name: Build Extensions

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout master branch
      uses: actions/checkout@v4
      with:
        ref: master

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew

    - name: Build all extensions
      run: ./gradlew assembleDebug

    - name: Find and collect .cs3 files
      run: |
        mkdir -p cs3_files
        echo "Searching for .cs3 files in build directories..."
        find . -path "*/build/outputs/*.cs3" -exec cp {} cs3_files/ \;
        echo "Also checking build.gradle.kts directories..."
        find . -name "*.cs3" -not -path "./cs3_files/*" -exec cp {} cs3_files/ \;
        echo "Found .cs3 files:"
        ls -la cs3_files/ || echo "No .cs3 files found!"

    - name: Checkout builds branch
      uses: actions/checkout@v4
      with:
        ref: builds
        path: builds_branch

    - name: Copy .cs3 files to builds branch
      run: |
        cd builds_branch
        rm -f *.cs3
        cd ..
        if ls cs3_files/*.cs3 1>/dev/null 2>&1; then
          cp cs3_files/*.cs3 builds_branch/
          echo "Updated .cs3 files:"
          ls -la builds_branch/*.cs3
        else
          echo "WARNING: No .cs3 files found, keeping existing files"
        fi

    - name: Update plugins.json URLs
      run: |
        cd builds_branch
        REPO_NAME="${{ github.repository }}"
        sed -i "s|NivinCNC/CNCVerse-Cloud-Stream-Extension|${REPO_NAME}|g" plugins.json
        sed -i "s|NivinCNC/CNCVerse-Cloud-Stream-Extension|${REPO_NAME}|g" CNC.json

    - name: Commit and push builds
      run: |
        cd builds_branch
        git config user.name "GitHub Actions"
        git config user.email "actions@github.com"
        git add -A
        if git diff --cached --quiet; then
          echo "No changes to commit"
        else
          git commit -m "Auto-build: Update .cs3 files from master"
          git push origin builds
        fi
